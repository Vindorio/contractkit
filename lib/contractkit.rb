# frozen_string_literal: true

require_relative "contractkit/version"

# Top-level namespace for contractkit. See README.md and docs/ for usage.
#
# This file is intentionally minimal — concrete classes (Client,
# Configuration, Opportunity, Award, etc.) are introduced by later
# milestones (M1: core clients, M2: data models). At v0.0.1 only the
# version constant is defined.
module Contractkit
end
